# Comment Policy

Minimal comments — this OVERRIDES default commenting habits. Comment only
non-obvious constraints or gotchas the code can't express. Never: banners,
change narration, boilerplate docstrings on trivial functions, commented-out
code, or restating what the code does. Prefer better names over comments.
Most functions get zero comments.
